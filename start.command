#!/usr/bin/env bash
# ==========================================================
# DocToMD - Launcher con Doppio Click per macOS Finder
# ==========================================================
cd "$(dirname "$0")"
bash ./run.sh
